{
	"name": "operon",
	"version": "1.5.0",
	"description": "Task management system for humans and agents with inline tasks, file tasks, filters, pipelines, pinned tasks, calendar views, Kanban views, recurrence, and time tracking.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"lint": "eslint . --quiet",
		"lint:report": "eslint .",
		"lint:strict": "eslint . --max-warnings 0",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"check": "npm run lint:strict && npm run build",
		"release:guard": "node scripts/release-guard.mjs",
		"check:local": "npm run check && npm run release:guard && npm run phase5:regression",
		"phase5:regression": "node scripts/run-phase5-regression.mjs"
	},
	"keywords": [],
	"author": "Hasan Yılmaz",
	"license": "GPL-3.0-or-later",
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@eslint/json": "^2.0.0",
		"@types/node": "^16.11.6",
		"@typescript-eslint/parser": "^8.61.0",
		"esbuild": "0.28.1",
		"eslint": "^10.5.0",
		"eslint-plugin-depend": "^1.5.0",
		"eslint-plugin-obsidianmd": "^0.3.0",
		"obsidian": "1.13.0",
		"tslib": "2.4.0",
		"typescript": "^5.4.5",
		"typescript-eslint": "^8.61.0"
	},
	"dependencies": {
		"@codemirror/language": "^6.12.3",
		"@codemirror/state": "6.5.0",
		"@codemirror/view": "6.38.6",
		"ical.js": "^2.2.1"
	}
}
